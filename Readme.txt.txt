--Open Front-end folder in workspace.

--Delete packagelock.json (if its there)

--Do npm i express -g

--then run node scripts/web-server.js

--localhost:8100



---------FOR API------------

--Import AngularApi in STS.

--For first run, uncomment 2 lines in main class(they're used to add the first record in the database so that a collection gets created in mongo db)

--Make sure mongo db server instance is up and running.

--Run as Spring boot project.  

->endpoint: localhost:8080/api/all  


